readline V
lexer into words and operators via quoting rule V
here_docs in /tmp/
history in shellptic_history
tilde expansion
parameter and variable expansion
filename expansion (*)
quote removal
redirections
is command builtin ?
is command in path ?
addhistory if ok
try execution
else execute as shell script
register in _
set exit status

awk (thibault)
arithmetic expansion (myself)
tetris (oc)
cyberpunk with ray tracing (why not ?)
