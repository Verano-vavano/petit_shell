NAME = ./libmath.a
SRCS = char_checking.c char_checking2.c clean_input.c \
	   do_math.c is_math.c
DEST = $(SRCS:.c=.o)
all: ${NAME}

.c.o:
	cc -Wall -Wextra -Werror -c $< -o ${<:.c=.o}

${NAME} : ${DEST};
	ar cr ${NAME} ${DEST}

clean:
	rm -f ${DEST}

fclean: clean
	rm -f ${NAME}

re: fclean all
